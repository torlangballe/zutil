//go:build js

package zhttp

import (
	"testing"
)

// Needs: https://github.com/agnivade/wasmbrowsertest
func TestNewRequest(t *testing.T) {
	// start := time.Now()
	// reader := bytes.NewReader([]byte{5, 2, 2})
	// var count int
	// for time.Since(start) < time.Minute*35 {
	// 	count++
	// 	_, err := http.NewRequest("POST", "http://10.150.0.176/qtt/zrpc?method=RPCCalls.GetUpdatedResourcesAndSetSent", reader)
	// 	if err != nil {
	// 		t.Error(count, err)
	// 		return
	// 	}
	// }
}
